steps:
  # Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/gke-app-repository/app:$COMMIT_SHA', '.']

  # Push the image to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/gke-app-repository/app:$COMMIT_SHA']

  # Deploy to GKE
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'container'
      - 'clusters'
      - 'get-credentials'
      - 'gke-cluster'
      - '--zone=asia-northeast1-a'
      - '--project=$PROJECT_ID'

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: kubectl
    args:
      - 'set'
      - 'image'
      - 'deployment/jjugccc-2025'
      - 'jjugccc-2025=asia-northeast1-docker.pkg.dev/$PROJECT_ID/gke-app-repository/app:$COMMIT_SHA'

images:
  - 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/gke-app-repository/app:$COMMIT_SHA'

timeout: '1800s'

options:
  machineType: 'E2_HIGHCPU_8'